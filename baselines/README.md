# Baseline Models

This directory contains baseline implementations used for comparison with SwaraNet.

Baseline architectures:

- CNN
- AudioNet
- Bi-GRU
- CNN-BiGRU
- CNN-BiGRU-Attention

These models were trained using the same dataset split and evaluation protocol as the proposed SwaraNet framework.
